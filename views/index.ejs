<!DOCTYPE html>
<html lang="en">
<head>
  <meta charset="UTF-8">
  <title><%= title || 'Quản lý sản phẩm' %></title>
  <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" rel="stylesheet">
  <style>
    body { padding-top: 70px; background-color: #f8f9fa; }
    .card-hover:hover { transform: scale(1.05); transition: 0.3s; }
  </style>
</head>
<body>

<nav class="navbar navbar-expand-lg navbar-dark bg-dark fixed-top">
  <div class="container">
    <a class="navbar-brand" href="/">ProductManager</a>
    <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav">
      <span class="navbar-toggler-icon"></span>
    </button>
    <div class="collapse navbar-collapse" id="navbarNav">
      <ul class="navbar-nav ms-auto">
        <% if(currentUser){ %>
          <li class="nav-item"><span class="navbar-text me-3">Xin chào, <%= currentUser.username %></span></li>
          <li class="nav-item"><a class="nav-link" href="/users/logout">Thoát</a></li>
        <% } else { %>
          <li class="nav-item"><a class="nav-link" href="/users/login">Đăng nhập</a></li>
          <li class="nav-item"><a class="nav-link" href="/users/register">Đăng ký</a></li>
        <% } %>
      </ul>
    </div>
  </div>
</nav>

<div class="container mt-5">
  <div class="text-center mb-5">
    <h1 class="display-4">Chào mừng đến Product Manager</h1>
    <p class="lead">Quản lý sản phẩm và nhà cung cấp một cách dễ dàng</p>
  </div>

  <div class="row g-4 justify-content-center">
    <div class="col-md-4">
      <div class="card card-hover text-center">
        <div class="card-body">
          <h5 class="card-title">Sản phẩm</h5>
          <p class="card-text">Xem danh sách, thêm mới, sửa hoặc xóa sản phẩm.</p>
          <a href="/products" class="btn btn-primary">Quản lý Sản phẩm</a>
        </div>
      </div>
    </div>

    <div class="col-md-4">
      <div class="card card-hover text-center">
        <div class="card-body">
          <h5 class="card-title">Nhà cung cấp</h5>
          <p class="card-text">Quản lý thông tin nhà cung cấp và liên kết sản phẩm.</p>
          <a href="/suppliers" class="btn btn-success">Quản lý Nhà cung cấp</a>
        </div>
      </div>
    </div>

    <% if(!currentUser){ %>
    <div class="col-md-4">
      <div class="card card-hover text-center">
        <div class="card-body">
          <h5 class="card-title">Tài khoản</h5>
          <p class="card-text">Đăng nhập hoặc đăng ký để quản lý hệ thống.</p>
          <a href="/users/login" class="btn btn-warning me-2">Đăng nhập</a>
          <a href="/users/register" class="btn btn-info">Đăng ký</a>
        </div>
      </div>
    </div>
    <% } %>
  </div>
</div>

<script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
